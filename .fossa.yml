version: 3

project:
  locator: SolaceProducts_terraform-solacebrokerappliance-queue-endpoint
  id: SolaceProducts_terraform-solacebrokerappliance-queue-endpoint
  name: terraform-solacebrokerappliance-queue-endpoint
  teams: []
  labels:
    - terraform

vendoredDependencies:
  forceRescans: false
  scanMethod: CLILicenseScan
  licenseScanPathFilters:
    exclude:
      - "./.git"
      - "./.github"

paths:
  exclude:
    - ./.git
    - ./.github

telemetry:
  scope: full
